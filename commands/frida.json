{
  "checks": [
    {
      "category": "FRIDA",
      "label": "Frida Server Running",
      "command": "ps -A | grep frida",
      "safe_pattern": "^$",
      "level": "critical",
      "description": "Frida server is a dynamic instrumentation toolkit commonly used for hooking",
      "empty_is_safe": true
    }
  ]
}
