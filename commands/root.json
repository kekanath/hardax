{
  "checks": [
    {
      "category": "ROOT",
      "label": "Systemless Root Path",
      "command": "ls /sbin | grep magisk",
      "safe_pattern": "^$",
      "level": "critical",
      "description": "Magisk binary indicates systemless root",
      "empty_is_safe": true
    }
  ]
}
