{
  "checks": [
    {
      "category": "ADB SECURITY",
      "label": "ADB Keys Present",
      "command": "[ -d /data/misc/adb ] && ls /data/misc/adb/*.pub 2>/dev/null | wc -l || echo 0",
      "safe_pattern": "^[1-9][0-9]*$",
      "level": "warning",
      "description": "ADB authorized keys should exist for trusted connections"
    }
  ]
}